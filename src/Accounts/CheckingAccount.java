package Accounts;

public class CheckingAccount extends Account {

    public CheckingAccount(String accountHolder, int idAccount, int currentBalance) {
        super(accountHolder, idAccount, currentBalance);
    }
}
